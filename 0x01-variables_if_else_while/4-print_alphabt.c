#include <stdio.h>
#include <unistd.h>
/**
 * main - Entry point
 * Description: prints lowercase alphabets
 * Return: Always 0 (success)
 **/
int main(void)
{
	int bc = 'a';
	while (bc <= 'z')

	{
	if (bc == 'e' || lw == 'q')
	{
	bc += 1;
	}

	else
	{
	putchar(bc);

	bc += 1;

	}

	}

	putchar('\n');

	return (0);
}
