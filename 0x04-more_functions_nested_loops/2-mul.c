#include "main.h"

/**
 * mul - multiply two numbers
 * Description: Multiplying two integers
 * @a: an int given by the main function
 * Return: mul
 */
int mul(int a, int b);
{
	int mul = (a * b)

	_putchar(mul);
		return (mul);
}
