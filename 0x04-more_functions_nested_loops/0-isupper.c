#include "main.h"

/**
 * 0-isupper.c - check if a character is upper case
 * @c: the integer value it receives
 * Return: 1 if true. 0 if false.
 */
int _isupet numberier(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
